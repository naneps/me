import 'package:flutter/material.dart';

class CoreSmallScreen extends StatelessWidget {
  const CoreSmallScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
